import { createTheme } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: '#9d0455'
    },
    secondary: {
      main: '#fff',
    },
    tertiary: {
      main:'#000',
    }
  },
});


export default theme